# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This is a newly initialized repository named "imagefront". The .gitignore indicates it's configured for Visual Studio/.NET development, but the codebase structure has not yet been established.

## Project Setup

The repository currently contains:
- README.md (minimal placeholder)
- .gitignore (Visual Studio/.NET template)

When code is added to this repository, update this file with:
- Build commands (e.g., `dotnet build`, `npm run build`)
- Test commands (e.g., `dotnet test`, `npm test`)
- Development server commands if applicable
- Key architectural patterns and project structure
